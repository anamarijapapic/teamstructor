{
    "devcert": {
      "default": [
        {
          "domain": "teamstructor.test",
          "keyDestination": "docker/nginx/ssl/key.pem",
          "certDestination": "docker/nginx/ssl/cert.pem",
          "caDestination": "docker/php/certificate-authority/certificate.cert"
        }
      ]
    },
    "jobs": {
      "devcert": [
        "devcert:default"
      ]
    }
}
